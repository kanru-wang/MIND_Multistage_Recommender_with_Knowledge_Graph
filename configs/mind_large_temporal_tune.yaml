extends: "mind_large_temporal_baseline.yaml"

run_name: "mind_large_temporal_hard_neg_v4"

artifacts:
  teacher_run_name: "mind_large_temporal_tune"

ranker:
  lr: 1.0e-4
  lr_sweep:
    lrs: [1.0e-4, 1.5e-4, 2.0e-4]
    teacher_run_name: "mind_large_temporal_tune"
    evaluate: true
  hard_negative_sampling:
    enabled: true
    hard_for_cold_users_only: true
